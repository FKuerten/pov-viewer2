package de.sitl.dev.pov.viewer2.api.scene;

import de.sitl.dev.pov.viewer2.api.imageSource.ImageSource;

/**
 * The scene describes what we actually view (the camera describes how we view
 * it). Currently a scene is just a name, the meaning depends on the
 * {@link ImageSource}.
 * 
 * @author Fabian K&uuml;rten
 */
public interface Scene {
    // intentionally empty
}
